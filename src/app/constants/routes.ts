export enum rootRoutes {
    MAIN = '/',
    ADS = '/ads'
};

export enum rootPaths {
    MAIN = '',
    ADS = 'ads'
};

export enum adsRoutes {
   MAIN = '/ads' 
};

export enum adsPaths {
    MAIN = '',
    EDIT = ':id'
}